狀態：初始為 [ ]、完成為 [x]
注意：狀態只能在測試通過後由流程更新。
測試類型：前端元素、function 邏輯、Mock API、驗證權限...等

---

## [ ] 【前端元素】渲染登入畫面基本元件
**範例輸入**：Mount `LoginScreen` (設定初始狀態)
**期待輸出**：
- 標題顯示 "建立新帳戶" (預設) 或 "歡迎回來"
- 顯示 Email 輸入框
- 顯示 密碼 輸入框
- 顯示 主要操作按鈕
- 顯示 切換模式按鈕
- 顯示 Google 社群登入按鈕

---

## [ ] 【互動邏輯】切換登入/註冊模式
**範例輸入**：點擊切換模式按鈕 ("已有帳號？點此登入" 或 "還沒有帳號？點此註冊")
**期待輸出**：
- 觸發 `setIsLoginMode` 狀態更新
- 畫面標題在 "歡迎回來" 與 "建立新帳戶" 之間切換
- 按鈕文字在 "登入" 與 "註冊" 之間切換

---

## [ ] 【互動邏輯】密碼顯示切換
**範例輸入**：點擊密碼欄位右側的眼睛圖示
**期待輸出**：
- 觸發 `setShowPassword` 狀態更新
- 密碼輸入框 `secureTextEntry` 屬性改變

---

## [ ] 【業務邏輯】Email 輸入更新
**範例輸入**：在 Email 欄位輸入文字
**期待輸出**：
- 呼叫 `setEmail` 更新狀態 (透過 `TextInput` 的 `onChangeText`)

---

## [ ] 【業務邏輯】密碼 輸入更新
**範例輸入**：在 密碼 欄位輸入文字
**期待輸出**：
- 呼叫 `setPassword` 更新狀態 (透過 `TextInput` 的 `onChangeText`)

---

## [ ] 【業務邏輯】執行 Email 認證 (登入/註冊)
**範例輸入**：點擊主要操作按鈕 ("登入" 或 "註冊")
**期待輸出**：
- 呼叫 Data Hook 中的 `actions.handleEmailAuth`

---

## [ ] 【業務邏輯】執行 Google 社群登入
**範例輸入**：點擊 Google 圖示按鈕
**期待輸出**：
- 呼叫 Data Hook 中的 `actions.handleSocialLogin` 並帶入 `actions.signInWithGoogle`
狀態：初始為 [ ]、完成為 [x]
注意：狀態只能在測試通過後由流程更新。
測試類型：前端元素、function 邏輯、Mock API、驗證權限...等

---

## [ ] 【畫面渲染】訪客用戶 UI 呈現
**範例輸入**：`user.isAnonymous` 為 `true`
**期待輸出**：顯示「訪客用戶」，且清單中只有「登入 / 註冊帳戶」選項

---

## [ ] 【畫面渲染】一般登入用戶 UI 呈現
**範例輸入**：`user` 有 `displayName` 且 `isAnonymous` 為 `false`
**期待輸出**：顯示用戶名稱，且清單中包含編輯資料、偏好設定、登出選項

---

## [ ] 【使用者互動】登出功能正常觸發
**範例輸入**：一般用戶點擊「登出」列表選項，`authService.signOutUser()` 回傳成功
**期待輸出**：調用 `showSuccess("已登出")`

---

## [ ] 【使用者互動】訪客登出防呆機制
**範例輸入**：訪客點擊登出（若不小心出現），`authService.signOutUser()` 回傳失敗
**期待輸出**：調用 `showWarning("提示", "訪客用戶無法登出...")`
